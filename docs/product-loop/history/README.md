# Release train history

One directory per shipped train, named `<YYYY-MM-DD>-v<version>`, holding the
final `TRAIN.md` (plan, specs, outcomes and retro), the researcher's `research.md`
and HARDEN's `verification.md`. Written by the RECORD phase of `/release-train`;
nothing here is edited after the fact.
